// Mirrors backend/src/constants/roles.js - single place to change on the
// frontend if a role is ever added/renamed.
export const ROLES = Object.freeze({
  USER: 'user',
  ADMIN: 'admin',
  SUPER_ADMIN: 'super-admin',
});
